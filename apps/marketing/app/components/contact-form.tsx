"use client";
import { useState } from "react";
export default function ContactForm() { const [sent, setSent] = useState(false); return <form className="contact-form" onSubmit={(event) => { event.preventDefault(); setSent(true); }} aria-label="Contato Hexxon"><label>Nome<input name="name" autoComplete="name" required placeholder="Como podemos chamar você?" /></label><label>E-mail profissional<input name="email" type="email" autoComplete="email" required placeholder="voce@laboratorio.com.br" /></label><label>Mensagem<textarea name="message" placeholder="Conte brevemente o contexto do seu laboratório." /></label><button className="hx-button" type="submit">{sent ? "Recebemos seu interesse" : "Iniciar conversa"}</button><p className="form-note" aria-live="polite">{sent ? "Este formulário é uma demonstração visual: nenhum dado foi enviado." : "Sem compromissos comerciais ou alegações de certificação nesta página."}</p></form>; }
